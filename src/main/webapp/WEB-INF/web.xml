<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Spring</display-name>

    <!-- Spring MVC Configuration -->
    <servlet>
        <servlet-name>springDispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextClass</param-name>
            <param-value>
                org.springframework.web.context.support.AnnotationConfigWebApplicationContext
            </param-value>
        </init-param>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>com.example.lazyco.backend.core.WebMVC.BackendWebConf</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
        <!-- multipart configurations -->
        <multipart-config>
            <max-file-size>10485760</max-file-size> <!-- 5MB -->
            <max-request-size>20971520</max-request-size> <!-- 20MB -->
            <file-size-threshold>1048576</file-size-threshold> <!-- 1MB -->
        </multipart-config>
    </servlet>
    <servlet-mapping>
        <servlet-name>springDispatcher</servlet-name>
        <!-- for Root Context / else /api/ -->
        <url-pattern>/</url-pattern>
    </servlet-mapping>
    <!-- End of Spring MVC Configuration -->

    <!-- Request Caching Filter Configuration -->
    <filter>
        <filter-name>requestCachingFilter</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>requestCachingFilter</filter-name>
        <!-- for Root Context /* else /api/*-->
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    <!-- End of Request Caching Filter Configuration -->

    <!-- Spring Security Configuration -->
    <filter>
        <filter-name>springSecurityFilterChain</filter-name>
        <filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>springSecurityFilterChain</filter-name>
        <!-- for Root Context /* else /api/*-->
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    <!-- End of Spring Security Configuration -->
</web-app>